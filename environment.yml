name: Scrape
channels:
  - defaults
dependencies:
  - python=3.10
  - requests
  - beautifulsoup4
  - lxml
  - python-dotenv
  - pandas
# - selenium==4.10.0, using pip install selenium

prefix: /Users/adam/anaconda3/envs/Scrape

# conda env export --from-history | save -f environment.yml